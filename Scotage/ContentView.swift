import SwiftUI

/// Role: Face. Window host. FaceChrome locks the rate face. ReviewScreen is applied after onboarding.
struct ContentView: View {
    var body: some View {
        FaceChrome()
    }
}

#Preview {
    ContentView()
}
