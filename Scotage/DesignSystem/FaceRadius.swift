import SwiftUI

/// Role: DesignSystem. Cards and sheets 28pt, chips 14pt. Material elevation. Never a second radius.
enum FaceRadius {
    static let card: CGFloat = 28
    static let chip: CGFloat = 14
}
